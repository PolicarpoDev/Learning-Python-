numero = input("Entre com um número inteiro para calcular a sua raíz quadrada: ")
numero = int(numero)

raiz_quadrada = numero ** 0.5
raiz = round(raiz_quadrada, 4)

print(f"A raíz quadrada de {numero} é {raiz_quadrada}.")
